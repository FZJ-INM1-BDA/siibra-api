image: python:3.8
stages:
  - test
  - deploy

before-script:
  script:
    - python -m pip install --upgrade pip

test:
  stage: test
  script:
    - pip install -r app/requirements.txt
    - pip install coverage
    - coverage run -m pytest
    - coverage html -d coverage
    - coverage xml
  artifacts:
    paths:
      - coverage/
    reports:
      cobertura: coverage.xml

openshift-build:
  stage: deploy
  script:
    - echo "This job triggers an openshift build."
