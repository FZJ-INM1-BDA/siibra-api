apiVersion: v1
kind: Service
metadata:
  name: {{ include "siibra-api-v4-worker.fullname" . }}
  labels:
    {{- include "siibra-api-v4-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "siibra-api-v4-worker.selectorLabels" . | nindent 4 }}
